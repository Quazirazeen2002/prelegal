# Copy to frontend/.env.local for local (non-Docker) development, where the
# frontend (`npm run dev`, :3000) and backend (`uv run uvicorn`, :8000) run as
# separate servers. Leave unset for the Docker build, where both are served
# from the same origin and a relative path is already correct.
NEXT_PUBLIC_API_URL=http://localhost:8000
